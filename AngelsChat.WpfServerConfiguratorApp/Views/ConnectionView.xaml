<UserControl x:Class="AngelsChat.WpfServerConfiguratorApp.Views.ConnectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AngelsChat.WpfServerConfiguratorApp.Views"
      mc:Ignorable="d" Background="Black" Foreground="#888888" FontSize="16" VerticalAlignment="Top">

    <Grid VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!--IP-->
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="4" Text="Ip" FontSize="16" VerticalAlignment="Top"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="4" Background="#1F1F1F" Foreground="#888888" FontSize="16" Text="{Binding Ip}" VerticalAlignment="Top" />

        <!--Port-->
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="4" Text="Порт" FontSize="16" VerticalAlignment="Top"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="4" Background="#1F1F1F" Foreground="#888888" FontSize="16" Text="{Binding Port}" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
