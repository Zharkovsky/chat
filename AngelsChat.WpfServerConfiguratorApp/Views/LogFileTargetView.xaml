<UserControl x:Class="AngelsChat.WpfServerConfiguratorApp.Views.LogFileTargetView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AngelsChat.WpfServerConfiguratorApp.Views"
      mc:Ignorable="d" >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Margin="4" Text="Путь" FontSize="16"/>
        <TextBox x:Name="Path" Grid.Column="1" Margin="4" Background="#1F1F1F" Foreground="#888888" FontSize="16" Text="{Binding FileSource}" />
    </Grid>
</UserControl>
