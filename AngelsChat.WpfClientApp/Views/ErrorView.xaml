<UserControl x:Class="AngelsChat.WpfClientApp.Views.ErrorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AngelsChat.WpfClientApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="70" d:DesignWidth="300" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0 0 0 20" Background="#ccc" Foreground="Black">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition MaxWidth="20"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding ErrorMessage}" FontSize="14" TextWrapping="Wrap"/>
        <Button Grid.Column="1" Content="X" Command="{Binding Close}" Height="20" Margin="5 0 0 0" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
